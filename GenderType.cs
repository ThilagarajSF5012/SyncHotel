namespace SyncHotel
{
    /// <summary>
    /// Enum GenderType <see cref="GenderType"/> is to give the Gendr Options to the user
    /// </summary>
    public enum GenderType
    {
        Male, Female, Transgender
    }
}