namespace SyncHotel
{
    /// <summary>
    /// Enum Room Type is used to give type of room available in the hotel
    /// </summary>
    public enum RoomTypes
    {
        Standard, Delux, Suit
    }
}